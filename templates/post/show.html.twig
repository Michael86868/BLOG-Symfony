{% extends 'base.html.twig' %}

{% block body %}
    <section class="container mt-5 approval">
        <div class="col-8 mx-auto">
            <div class="col-xl-12">
                <img src="{{ asset('images/blogzk.jpg') }}" alt="Image" class="mx-auto d-block">
            </div>
            <div class="col-xl-12 text-center mt-3">
                <div class="d-flex justify-content-between text-muted h6 mb-5">
                    <span class="ms-5 fw-bolder"><span class="fw-bolder fa fa-user-circle-o me-2"></span>{{ post.author.username }}</span>
                    <span class="mx-5 fw-bolder"><span class="fw-bolder fa fa-eye me-2 "></span>500</span>
                    <span class="me-5 fw-bolder"><span class="fw-bolder fa fa-calendar-o me-2"></span>{{ post.createdAt|date('Y-m-d') }}</span>
                </div>
                <h1 class="text-center mt-5">{{ post.title }}</h1>
                <div class="row px-5">
                    {{ post.content|raw }}
                </div>
                <p class="text-center text-muted mt-5" style="font-size: 20px; font-family: 'Lora', serif;">
                    <i>by</i>
                    <a class="text-decoration-none text-dark" href="{{ path('user_show', {'id': post.author.id}) }}">{{ post.author.username }}</a>
                    <span class="date">| {{ post.createdAt|date('d.m.Y') }}</span>
                </p>
                <div class="row row-cols-1 row-cols-lg-auto g-4 ms-0 mt-3 mb-0 px-6 filter-tags px-5">
                    <div class="col bg-danger">
                        <span>TAGS<span class="ms-2 fa fa-tag"></span></span>
                    </div>
                    {% for tag in post.tags %}
                        <span class="col tag px-0 ms-2">
                            <span class="btn px-2 py-0 align-baseline fw-bolder">{{ tag }}</span>
                         </span>
                    {% endfor %}
                </div>
                <hr class="mt-5">
            </div>
            <div class="mx-auto text-center mt-5">
                <a href="{{ path('post_index') }}" class="btn btn-primary p-2 px-3">Zpátky k článkům</a>
            </div>
        </div>
    </section>
{% endblock %}
